{
  "framework": "nextjs",
  "installCommand": "(corepack enable || true) && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter web build",
  "devCommand": "pnpm --filter web dev",
  "outputDirectory": "apps/web/.next",
  "nodeVersion": "18.x"
}
